# Production & Development Environment Variables
PORT=5000
NODE_ENV=production
JWT_SECRET=academix-production-jwt-secret-key-change-me-2026!

# Database connection (SQLite by default, or PostgreSQL string)
# DATABASE_URL="postgresql://user:password@localhost:5432/academix?schema=public"
DATABASE_URL="file:./dev.db"
